# Changelog

## [4.1.9] - 2026-08-06

### Added
- Windows 通知区域菜单和 macOS 菜单栏菜单新增只读版本项，显示为“版本：v4.1.9”。
- 菜单版本直接读取编译时 `CODEXPETS_VERSION`，后续修改统一 `VERSION` 文件即可自动同步。

### Tests
- Windows x64 原生构建、核心测试、资源校验及普通/启动冒烟测试通过。


## [4.1.8] - 2026-08-06

### Changed
- Windows 云朵标题优先使用幼圆，macOS 使用内置系统圆角字体；两端均保留可靠的系统字体回退。
- 云朵正文改用更清晰的中文半粗体，适当增大字号、行高并加深文字颜色，改善小尺寸下的可读性。
- Windows 将 GDI+ 字体对象纳入渲染器生命周期管理；macOS 避免查询可下载字体，防止无界面打包环境阻塞字体激活。
- 同步更新 README 使用的 15 张桌宠状态截图，展示新的云朵文字效果。

### Tests
- Windows x64 原生构建、核心测试、资源校验、普通/启动冒烟测试及 15 张状态预览通过。


## [4.1.7] - 2026-08-06

### Changed
- 托盘状态菜单改为每个活跃任务独立一行，不再用分号拼接；格式为“项目名称：进行中（步骤）”。
- 移除托盘提示中的 “CodeXPets” 前缀，提示内容直接展示当前状态行。

### Tests
- 新增状态行列表与多行状态文本的核心回归测试。


## [4.1.6] - 2026-08-06

### Changed
- 状态栏不再合并所有活跃任务的进度；现在逐项显示为“项目名称：进行中（步骤）”，多个任务以分号分隔。
- 项目名称缺失时回退显示任务标题，避免状态栏出现无来源的汇总状态。

### Tests
- 新增逐项目状态文本及缺失项目名回退的核心回归测试。


## [4.1.5] - 2026-08-06

### Fixed
- 修复小爱播报在同一轮监听到多个任务事件时可能错配任务标签的问题：任务 A 的“开始工作”不会再播报成任务 B。
- 监听更新合并时保留每个事件对应的项目标签，避免最新快照覆盖较早事件的播报上下文。

### Tests
- 新增多会话并发任务开始、小爱播报上下文及更新队列事件上下文合并回归测试。


## [4.1.4] - 2026-08-06

### Changed
- 小爱登录授权校验、设备扫描和测试播报改为后台异步执行，避免网络请求阻塞设置界面。
- Windows 和 macOS 在后台任务完成后统一回到 UI 主线程更新设置、设备列表和提示框，并在操作期间禁用重复操作。

### Tests
- Windows x64 原生构建、核心测试、资源校验和异步回调测试通过。

## [4.1.3] - 2026-08-06

### Changed
- Windows 小爱音箱选择器改为紧凑的下拉式多选菜单：主界面显示已选摘要，展开后可逐项勾选或一键全选，不再常驻显示大列表。

### Tests
- Windows x64 原生构建、核心测试和资源校验通过。

## [4.1.2] - 2026-08-06

### Added
- 小爱音箱目标设备支持多选；Windows 和 macOS 设置界面均提供设备列表与“全选”操作。
- 小爱播报支持按可配置上限分批并发发送，默认同时推送 3 台音箱；设置可在 1–8 台之间调整。

### Changed
- 设置文件新增 `XiaoAi.DeviceIds` 和 `XiaoAi.MaxParallelRequests`，并继续兼容原有单设备 `DeviceId` 配置。
- 多设备播报会等待同批请求结束，并在失败时汇总具体设备与错误原因。

### Fixed
- macOS 浏览器登录验证成功后会自动启用小爱主动播报，与 Windows 行为保持一致。
- 小爱自动播报待处理任务限制为 16 条，避免网络异常时通知无限积压。

### Tests
- 新增多设备设置持久化、设备去重及 5 台设备受限并发（峰值 3）播报测试。

## [4.1.1] - 2026-08-05

### Fixed
- 修复 Windows WebView2 登录回调释放时的堆损坏问题。
- 延迟销毁 WebView2 控制器，避免登录成功后扫描音箱时发生访问冲突。


## [4.1.0] - 2026-08-05

### Added
- Windows 和 macOS 统一支持小爱音箱主动播报：共享 MiNA 核心、设备扫描、TTS、授权校验和事件文案；Windows 使用 WebView2，macOS 使用 WKWebView。
- Windows WebView2 loader 按 x64/ARM64 嵌入单 EXE，发布包不再需要旁置 loader DLL。

### Changed
- 将监控事件副作用收敛到共享策略和有界更新队列，统一 Windows/macOS 的开始、完成、异常和中断行为，并用 generation 隔离 sessions 目录切换后的旧回调。
- 登录流程改用一次性非持久化浏览器会话；Windows 临时 WebView2 profile 在登录结束后异步清理，macOS 使用非持久化 WebKit 数据存储。
- 缩小小爱设备公共 DTO，删除未使用的会话监控 API、平台辅助函数和无用 include。
- 删除未引用的旧云朵资源，并增强 Windows 启动冒烟和 macOS 打包临时目录清理。
- CI/CD 升级 Actions 版本，发布前严格检查 8 个平台文件，发布后下载并验证 9 个资产及 SHA-256 校验和。

### Tests
- Windows x64/ARM64、macOS Intel/Apple Silicon 均执行原生构建、核心测试、资源校验、冒烟测试、预览图生成和打包；发布 workflow 另行验证正式 Release 的资产集合、标签和校验和。
## [4.0.3] - 2026-08-05

### Fixed
- 修正 macOS 翻转 `PetView` 与未翻转父视图之间的 `hitTest:` 坐标转换，猫的头部、身体、腿、尾巴和云朵现在都能从第一次按下直接开始拖动。
- 将 macOS 宠物窗口改为非激活面板，并通过全局鼠标移动监控即时切换透明区域穿透，避免鼠标移入后第一次点击被下层窗口接收。
- macOS 图片缓存改为有界复用当前动画帧组，隐藏云朵、切换悬浮/吸附姿态和退出时继续主动释放临时图片。
- macOS 不再生成界面未使用的周期诊断快照，监控快照与展示字符串改用移动语义，减少长期运行中的复制和分配。
- 大型非事件 JSONL 记录不再构建完整 JSON DOM；初始会话恢复扫描完成后主动归还空闲堆页，降低 macOS 常驻内存高水位。

### Tests
- 14 组原生核心测试、Windows x64 打包，以及 macOS Intel/Apple Silicon 与 Windows x64/ARM64 CI 构建、冒烟测试和打包全部通过。

## [4.0.2] - 2026-08-05

### Fixed
- macOS 桌宠现在使用完整精灵目标区域作为拖动命中区，从猫头、身体、腿和尾巴均可开始拖动；云朵任务切换点击仍保持不变。
- macOS 资源校验改为只读取 PNG 元数据，运行时图片缓存改为按状态和动画帧有界复用；隐藏云朵、隐藏宠物、播放结束和退出时会主动释放临时图片/音频资源。
- 降低 macOS UI 定时器和无效重绘频率，减少长期运行时的图形分配压力。
- 会话监听器不再因待完成工具调用或仍由 Codex 写入的长任务过早判定为空闲，并释放超大 JSON 行的长期缓冲容量。
- 新增拖动布局、长任务、工具调用和超大 JSON 行回归测试。

## [4.0.1] - 2026-08-05

### Fixed
- macOS 预览改为通过翻转的 AppKit 视图离屏绘制，修复预览图整体和文字上下颠倒；Windows/macOS 的预览状态现在也复用同一套实际展示文案，CI 会保留五种浮动和左右吸附状态截图。
- macOS 改用与 Windows 同一份 540×220 像素云朵资源，并共用气泡位置、连接点、精灵落点、可见区域和文本布局合同；不再显示成圆角矢量框。
- macOS 透明桌宠窗口现在可成为键盘焦点并主动保持拖拽交互；吸附状态补齐路径相交唤出，修复点击到猫或云朵后无法拖动的问题。
- macOS 和 Windows 都将监听器快照合并为有界待处理队列；macOS 不再为每次监听更新排入主线程 block，Windows 也只保留一条待处理的 UI 唤醒消息，降低长期运行时的主线程/消息队列积压、图形内存和分配压力。
- 统一 Busy、Completed、Error、Interrupted 和 Idle 的展示文案及中断状态处理；macOS 现已支持 Windows 同样的 `--expression-demo` 五状态轮换。
- 为共享展示与布局合同新增原生核心测试，防止后续 Windows/macOS 样式和交互再漂移。

## [4.0.0] - 2026-08-04

### 完全原生重构

- 将 .NET 10、Avalonia、SkiaSharp 和托管运行时全部替换为 C++20 共享核心。
- Windows 改用 Win32/GDI+；macOS 改用 AppKit/Core Graphics；不再携带第三方 UI 运行时。
- 会话监控改为有界的 500 ms 轻量轮询和 JSONL 增量读取，保留任务生命周期、异常、标题和计划进度识别。
- 保留透明桌宠、云朵文本、多任务切换、拖动、吸附、自动隐藏、多显示器位置、设置、诊断、声音、登录启动、更新入口和旧设置迁移。

### 体积与资源

- 精灵资源拆分为单帧 PNG；Windows 和 macOS 统一携带同一份 540×220 像素云朵运行时资源。
- Windows Release 原生 EXE 约 1.3 MiB；发布脚本对 EXE、APP、ZIP、DMG 强制执行 10 MiB 上限。
- 去除 macOS JIT entitlement，使用 Core Graphics bitmap context 生成预览。

### 构建与验证

- 新增 CMake、原生核心测试、Windows/macOS 资源校验和离屏冒烟命令。
- CI/CD 改为 Windows x64/ARM64、macOS Intel/Apple Silicon 原生 CMake 构建。
- 发布流程继续支持 ad-hoc/Developer ID 签名、Apple 公证、DMG、ZIP 和 SHA-256。
- 统一 MSVC 静态运行时设置，自动工具链会根据目标架构选择 Zig 或 MSVC。
- 新增隔离配置的 Windows 真实 GUI 启动冒烟测试，不再只依赖离屏渲染判断应用可启动。

### 原生回归修复

- 恢复吸附状态下任务云朵的定时隐藏规则；鼠标悬停只负责唤出宠物，不再错误延长内容计时。
- 修复 Windows 云朵位于猫下方时的点击坐标、多任务选择保持和托盘忙碌图标循环。
- 切换 sessions 目录时隔离旧监听器的排队快照，并为诊断窗口增加低频状态快照。
- 修复 Windows 在 `WM_NCCREATE` 阶段使用尚未赋值的成员窗口句柄，导致直接启动提示“创建后台消息窗口失败”的问题；窗口创建失败时现在会附带具体 Win32 错误码。
- 恢复旧版云朵像素外观、浮动猫按不透明像素锚点落地，以及浮动/左右吸附状态下思考连接点的原始尺寸、粗边框和等间距布局。
- 将 Zig Windows 产物显式链接为 GUI 子系统，消除启动时出现的黑色控制台窗口。

## [3.0.6] - 2026-08-03

### 新增
- 透明区域点击穿透：只保留猫、云朵和思考连接点作为可点击像素级区域，其余透明区域点击穿透到下层应用。
- 云朵气泡标题分隔符 `•` 左右增加空格。
- 左/右吸附时猫头与云朵之间的两个思考点镜像对称错位。

### 性能
- 点击区域使用预计算像素段，避免逐帧扫描；增加 200ms 区域更新节流。
- 移除镜像猫精灵图副本，云朵贴图启动时降采样，降低内存占用。

# 更新日志

## [3.0.5] - 2026-08-03

### 监控架构

- 统一使用 `FileSystemWatcher + JSONL` 增量监听作为唯一任务状态来源，直接读取 Codex sessions 目录，无需额外服务或启动参数。
- 文件事件使用约 25 毫秒防抖和约 120 毫秒最小轮询间隔合并，并保留每 30 秒一次的恢复轮询。
- 会话解析器按文件位置读取新增字节，支持跨读取块 UTF-8 解码、不完整 JSONL 行缓冲和文件截断重置。
- 快速发现当天及前两天的会话文件，每 120 秒执行完整发现，并最多跟踪最近修改的 40 个文件。
- 任务开始、完成、异常、标题和计划进度统一归约为监控快照，再由 Avalonia UI 线程更新桌宠、托盘和提示音。

### 清理

- 当前文档统一为 `.NET 10 + Avalonia + FileSystemWatcher/JSONL 增量监听` 单一方案。
- 删除未实现监控方案的使用说明、架构数据流、配置项和诊断命令描述，避免与实际代码不一致。

## [3.0.4] - 2026-08-03

### 修复

- 空闲状态始终显示与旧 Windows 原型一致的像素灯泡，并将空闲文案固定为“主人，现在没有在进行中的任务!别让我歇着!”。
- 设置窗口将“吸附隐藏（秒，0=关闭）”固定为单行，避免窄窗口换行。
- 桌宠资源统一为纯白身体、黑色耳朵、黑色尾巴和黄色眼睛；同步修复应用图标中的白色矩形背景与错误眼睛颜色。
- 重新调整灯泡、云朵下方思考点和猫头之间的间距；任务栏状态点扩大到清晰可见的 64 像素图标范围。

### 清理

- 删除旧 WinForms、Swift/AppKit 双架构、重复源资源、旧自测程序和无用编译/审计中间文件；正式仓库只保留 .NET + Avalonia 一套实现。
- Windows 与 macOS 打包脚本会在成功生成正式产物后删除 stage、bundle、挂载点和临时图标目录。

### 验证

- 资源校验新增 AppIcon 配色/背景、状态点不透明尺寸和纯色阈值验证。
- Windows x64/ARM64 继续生成 self-contained 单文件 EXE；macOS x64/ARM64 继续生成标准 app、ZIP 和 DMG。

## [3.0.3] - 2026-08-03

### 修复

- 修复 Windows 单文件版语音无声：MCI MP3 驱动改为在专用 STA 线程中打开、等待播放结束并关闭，开始、完成和异常三段语音均可独立诊断。
- 云朵调整为更短、更高的比例，并按云朵资源真实不透明边界布置内部元素，避免透明留白造成视觉错位。
- 将忙碌、完成和异常状态图标恢复为旧 Windows 版的 13 × 14 像素灯泡，保留深棕轮廓、灯丝、灯座、高光和五组像素光芒。
- 重新定位灯泡与两枚思考点：灯泡稳定处于云朵左侧安全区；大点贴近云朵，小点贴近猫头，并根据浮动/左右扒边姿势动态连接。

### 验证

- 新增 `--test-sound [start|complete|error]` 实际播放诊断入口。
- 全部 22 项核心测试、资源合同、离屏状态预览和 Windows x64/ARM64 单文件打包校验保持通过。

## [3.0.2] - 2026-08-03

### 架构重构

- 将原 WinForms C# 与 Swift/AppKit 双实现重构为 `.NET 10 LTS + Avalonia 12.1.1` 单一 C# 产品代码库。
- 新增 `CodeXPets.Core`、`CodeXPets.App` 和 xUnit 测试工程；会话监控、路径、设置、状态机和几何规则与 UI/平台 API 解耦。
- Windows 与 macOS 仅保留薄平台适配层，统一托盘/菜单栏、透明桌宠、云朵、多任务、吸附、诊断和设置行为。
- `VERSION` 成为程序集、应用包和发布文件的单一版本来源。

### 平台与架构

- 新增 Windows x64、Windows ARM64、macOS Intel x64、macOS Apple Silicon ARM64 四个独立 self-contained 发布目标。
- Windows 打包校验 PE machine，macOS 打包校验 apphost 和 bundle 内所有 Mach-O 架构。
- GitHub Actions 改为四个原生 runner 分别构建、测试、运行资源校验、执行原生冒烟测试并上传对应产物。
- macOS 包改为标准 `CodeXPets.app`，支持 Developer ID hardened runtime、最小 JIT entitlement、应用/DMG 公证与 staple。

### 功能还原

- 迁移原 Windows JSONL 增量读取、UTF-8 跨块解码、任务开始/完成/中断、异常完成、计划进度、旧日期会话发现和失联任务清理逻辑。
- 还原托盘/菜单栏状态、透明置顶不抢焦点窗口、长文本滚动、多任务自动和手动切换。
- 还原多显示器位置、左右吸附、自动隐藏、固定高度局部边缘唤出、拖动解除吸附和屏幕内朝向。
- 还原设置、诊断、声音、开机/登录启动、会话目录和查看更新入口。
- 新设置统一为平台用户数据目录中的 JSON，并自动迁移旧 Windows 注册表或 macOS `UserDefaults`。

### 角色与资源

- 桌宠修正为卡通化白色英国短毛猫：白色身体、黑色耳朵、黑色尾巴、金黄色眼睛。
- 保持原动作帧、精灵表坐标、透明度和左右扒边姿势不变。
- 新增资源合同校验：尺寸、透明背景、白身/黑耳/黑尾/金黄色眼睛像素阈值、图标和音频完整性。

### 测试与打包

- 新增路径隔离、`CODEX_HOME`、设置归一化、状态选择、任务手动选择保持、精灵索引、会话生命周期、异常分类和动态 sessions 目录测试。
- 新增 `--validate-resources`、`--smoke-test`、`--preview` 和 `--version` 工具命令。
- Windows x64/ARM64 改为压缩 self-contained 单 EXE，直接下载即可运行；ZIP 备份中也只包含一个 `CodeXPets.exe`。
- Windows 与 macOS 脚本生成架构独立 EXE/ZIP/DMG 和 SHA-256；发布包移除调试符号。
- 旧 WinForms 与 Swift/AppKit 实现已在 3.0.4 清理，正式发版只使用统一 C# 架构。

## [3.0.1] - 2026-08-03

### 新增

- 新增标准 macOS DMG 安装镜像，镜像内包含 `CodeXPets.app` 和 `Applications` 快捷入口，可直接拖拽安装。
- 保留 Universal 2 ZIP 作为备用下载方式。

### 测试

- macOS 构建流程会自动验证 DMG 文件系统、挂载内容、应用版本、Applications 快捷入口、arm64 + x86_64 架构、资源完整性和 ad-hoc 代码签名。
- CI 与正式发版工作流同步上传 DMG、ZIP，并将两种包纳入 SHA-256 校验。

## [3.0.0] - 2026-08-02

### 新增

- 新增全功能 macOS 原生版：菜单栏状态、透明桌面宠物、云朵任务提示、多会话轮换、左右吸附、自动隐藏、局部边缘唤出、多显示器位置记忆、设置、诊断、登录启动和内嵌语音提醒。
- macOS 版使用 AppKit 与 Swift Package Manager 实现，发布 Apple Silicon + Intel Universal 2 应用包。
- 新增 macOS 会话监测核心与自动化测试，覆盖任务生命周期、计划进度、异常完成、UTF-8 跨读取块、旧日期会话、冻结修改时间和失联任务判断。
- 新增 Windows/macOS 双平台 CI 和标签发版工作流，自动生成便携包与统一 SHA-256 校验文件。

### 修复

- 修复 Windows 吸附状态下点击云朵无法稳定切换任务的问题：吸附时整朵云均可点击，手动选择不会再被周期刷新立即重置，点击后会延长云朵显示时间。
- macOS 版从首个版本即采用相同的一次性“最新任务聚焦”规则，避免同类问题。

### 变更

- 项目从 Windows 单平台升级为 Windows 与 macOS 双平台，版本提升至 `3.0.0`。
- 根目录新增统一 `VERSION` 文件；Windows 程序、macOS 应用、文档和发布包版本保持一致。

本项目的正式版本变更记录。

## [2.2.1] - 2026-08-02

### 变更

- 将默认边缘触发区高度从 160 像素调整为 240 像素。


## [2.2.0] - 2026-08-02

### 补充优化

- 最后发生变化的任务会同步成为云朵当前显示内容，不再停留在之前选中的会话。
- 异常提示会显示 `任务失败：任务标题`，并保证异常云朵提示至少保留 10 秒。
- 动画帧、正文滚动和停靠动画都没有变化时跳过云朵重绘，降低长时间运行时的无效 CPU 消耗。
- 忙碌标题统一显示为 `进行中(当前步骤/总步骤)·会话(当前会话/总会话)`。

### 新增

- 新增托盘“设置…”窗口，可配置吸附触发区高度、自动隐藏时间、唤出保持时间、任务云朵时间、语音提醒和会话目录。
- 新增“诊断信息…”窗口，显示监听目录、跟踪文件、活跃任务、最近事件以及读取和解析错误，并支持复制。
- 新增“查看更新…”入口，打开项目最新发布页面。
- 新增 `package.ps1`，生成便携 ZIP 和 SHA-256 校验文件；新增 `create-shortcuts.ps1` 创建开始菜单和桌面快捷方式。

### 优化

- 隐藏后的吸附宠物只在最后吸附高度附近的固定高度局部边缘触发，不再响应整条屏幕边缘。
- 会话读取改用有状态 UTF-8 解码器，避免中文或 emoji 跨 64 KB 读取块时损坏。
- 初始和周期扫描会包含最近修改的旧日期会话，支持持续多天的任务，并限制跟踪文件数量。
- 失联任务改为依据任务和会话文件的活动时间清理，不再依赖固定的 `codex.exe` 进程名，更适合桌面端工作流。
- JSON 事件改为结构化解析，兼容更灵活的空白格式，并增加最大事件行缓冲。
- 动画刷新从约 60 FPS 调整为约 30 FPS，托盘状态与布局刷新按状态节流，降低空闲 CPU 唤醒和对象分配。
- 声音开关和显示行为设置会持久化到当前用户配置。

### 测试

- 新增旧日期活跃会话、UTF-8 跨块、部分 JSON、失联任务判定、配置归一化、自定义吸附区域和发布版本信息测试。

## [2.1.0] - 2026-08-02

### 新增

- 桌面宠物支持吸附到屏幕左侧和右侧，并自动切换为对应方向的小尺寸半头探出、前爪扒边姿势。
- 吸附状态下，仅在新任务、任务完成、进度更新或任务异常时短暂展示思考云朵。
- 仅吸附状态在连续 10 秒没有任务内容变化时滑出隐藏；未吸附状态始终保持显示。
- 吸附姿势保持固定，仅快速切换双眨眼、完成开心和异常流泪表情。
- 小猫隐藏后，吸附侧整条屏幕边缘均可触发快速滑入，并在鼠标移开后继续显示约 3 秒。
- 按下或拖动吸附宠物时暂停隐藏，拖动结束后重新开始 10 秒计时，避免拖到“空气”。
- 补充服务端错误、请求失败、流中断及异常完成消息识别，避免错误任务被标记为完成。
- 移除顶部吸附。
- 新增位置持久化：记住普通位置、所在显示器、左右吸附方向及垂直位置，并适配分辨率和显示器变化。
- 未吸附状态会根据所在屏幕的左半边或右半边自动镜像，所有任务状态都朝向屏幕内部。
- 再次拖动猫头即可解除吸附并恢复完整宠物。

### 变更

- 桌面宠物固定为黑耳白猫。
- 删除奶茶小水獭资源、动物皮肤系统、换肤菜单和皮肤选择保存逻辑。
- 清理未使用的运行时图标资源、重复构建参数和冗余状态判断。
- 精简白猫精灵表，只保留实际使用的四行动画，画面内容保持不变。

## [2.0.1] - 2026-08-02

### 新增

- Windows 单文件便携版桌面宠物，无需安装。
- 自动监测 Codex CLI 会话的空闲、进行中、完成和异常状态。
- 托盘状态图标、任务标题提示和桌面云朵气泡。
- 长任务内容自动换行、滚动及多任务轮换显示。
- 开始、完成和异常事件的内嵌语音提示。
- 托盘菜单支持开机自动运行、打开 Codex 会话目录和退出程序。
- 自测程序覆盖会话状态、事件通知、资源嵌入和图标渲染。

### 变更

- 应用名称统一为 `CodeXPets`。
